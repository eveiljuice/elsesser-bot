# 📘 Project Best Practices

## 1. Project Purpose
Telegram-бот для продажи рационов питания и FMD-протокола с калькулятором калорий, системой верификации оплат через админ-канал, follow-up/авто-рассылками и планировщиком фоновых задач. Технологический стек: Python 3.10+, aiogram 3.22, aiosqlite, APScheduler 4.x.

## 2. Project Structure
- bot.py — точка входа: инициализация бота, диспетчера, роутеров, scheduler и фона
- database.py — слой данных: модели/схемы таблиц, CRUD, аналитика, лог событий
- handlers/ — обработчики (роутеры aiogram)
  - handlers/user.py — пользовательские сценарии: /start, оплата, выдача рационов и FMD
  - handlers/admin.py — админ-панель, модерация оплат, статистика, рассылки/шаблоны
  - handlers/calculator.py — калькулятор калорий и FSM для многошаговых диалогов
- keyboards/ — reply/inline-клавиатуры, callback-данные (callbacks.py)
- data/ — контент и рецепты (RECIPES и FMD_RECIPES)
- followup.py — follow-up/автоматические рассылки (логика триггеров, отмена, планировщик)
- config.py — конфигурация и константы (токены, цены, ADMIN_USERNAMES и т.д.)
- requirements.txt — зависимости
- README.md / AGENTS.md — документация проекта, dev/run инструкции

Conventions:
- handlers и keyboards разделены по доменам (user/admin/calculator)
- database.py — единая точка для работы с БД и аналитикой
- Follow-up и Broadсast системы — фоновые задачи через APScheduler

## 3. Test Strategy
Current: тестов нет в репозитории. Рекомендуемая стратегия:
- Framework: pytest + pytest-asyncio
- Структура: tests/ зеркалирует проект (tests/handlers/, tests/database/, tests/followup/)
- Типы тестов:
  - Unit: чистая бизнес-логика, функции утилит, вычисления калькулятора
  - Integration: взаимодействие с БД (aiosqlite in-memory), FSM, роутеры aiogram (с тестовым Bot)
- Mocking:
  - Telegram API: мокать отправку сообщений/медиа; проверять сформированный текст/кнопки
  - Время/планировщик: freezegun или фикстуры времени; запуск задач вручную
  - База: in-memory SQLite (sqlite3=:memory:) + миграции/инициализация в фикстурах
- Naming/Conventions:
  - test_*.py, функции test_*; Given-When-Then структура описания
  - Фикстуры: event_loop, bot_client, tmp_db_path, sample_user
- Coverage expectations:
  - 80%+ для критичных модулей (database, followup, платежные сценарии)

## 4. Code Style
- Python 3.10+, типизация где возможно (PEP 484). Подсказки типов упрощают рефакторинг
- Именование: snake_case для функций/переменных, PascalCase для классов, модули — lower_snake
- Async/await:
  - Все операции с БД и сетью — асинхронные
  - Не блокировать event loop: избегать синхронных операций I/O; при необходимости — to_thread
- Логирование: использовать logging.getLogger(__name__), уровни INFO/WARNING/ERROR
- Докстринги и комментарии: краткие, объясняют «зачем», а не «что»; публичные функции — docstring
- Сообщения Telegram:
  - Использовать HTML ParseMode; экранировать пользовательский ввод
  - Сегментировать длинные сообщения (лимит Telegram)
- FSM: хранить состояние диалогов в памяти/БД по необходимости; чётко завершать состояния
- Ошибки и исключения:
  - Оборачивать API-вызовы Telegram в try/except (например, RetryAfter, BadRequest) с бэкоффом
  - Операции БД — через контекстные менеджеры, транзакции при пакетных изменениях
  - В задачах планировщика ловить и логировать исключения, не падать молча

## 5. Common Patterns
- Router-based архитектура (aiogram):
  - Каждый модуль-обработчик регистрирует свой Router; точка сборки — bot.py
- Database gateway:
  - Один модуль database.py инкапсулирует схему и доступ (CRUD, аналитика, события)
  - EventType enum + log_event() для бизнес-аналитики (START_COMMAND, PAYMENT_* и т.д.)
- Products separation:
  - Два независимых продукта: «Рационы (14 дней)» и «FMD протокол (5 дней)»
  - Отдельные цены, флаги доступа в БД (has_paid, has_paid_fmd), product_type в платежах
- Follow-up/Auto-broadcast:
  - Триггерные рассылки по событиям/статусам пользователя
  - Планировщик APScheduler запускает периодические задачи проверки и отправки
- Keyboards/Callbacks:
  - Единое место для callback-данных и конструкторов inline/reply клавиатур
- Config-first:
  - .env + config.py: токены, ADMIN_USERNAMES, цены, ID каналов, и т.п.

## 6. Do's and Don'ts
- ✅ Делайте
  - Разделяйте домены: user/admin/calculator — в отд��льных роутерах
  - Описывайте новые события в EventType и логируйте ключевые действия пользователей
  - Пишите асинхронные функции для БД и внешних API
  - Используйте шаблоны сообщений и клавиатуры из keyboards/
  - Валидируйте пользовательский ввод (числа/единицы) в калькуляторе
  - Логируйте все ошибки и важные бизнес-события
  - Добавляйте конфигурацию в .env и прокидывайте через config.py
  - Документируйте новые команды/кнопки в README/AGENTS.md
- ❌ Избегайте
  - Блокирующих операций (включая файловый I/O) в хэндлерах
  - Дублирования логики: выносите общие функции в utils или database.py
  - Жёстко закодированных токенов/ID в коде — храните в .env
  - Тихого игнорирования исключений в планировщике/хэндлерах
  - Изменения схемы БД без миграции/инициализации и без обновления database.py

## 7. Tools & Dependencies
- Python 3.10+
- aiogram 3.22 — Telegram Bot API
- aiosqlite — асинхронная работа с SQLite
- APScheduler 4.x — фоновые и периодические задачи
- Дополнительно: python-dotenv (если используется), logging (стандартный модуль)

Setup/Run:
- venv: python -m venv venv && venv\Scripts\activate (Windows)
- Dependencies: pip install -r requirements.txt
- .env: BOT_TOKEN, ADMIN_CHANNEL_ID, PAYMENT_AMOUNT, FMD_PAYMENT_AMOUNT, PAYMENT_DETAILS, ADMIN_USERNAMES
- Запуск: python bot.py (БД создаётся автоматически при первом запуске)

## 8. Other Notes
- Сообщения должны быть совместимы с HTML ParseMode, экранируйте ввод пользователя
- Telegram лимиты: учитывайте ограничения на длину текста и частоту отправки
- Broadcast/Auto-broadcast: планировщик проверяет pending/triggered задачи каждые N минут
- Timezone: планирование рассылок — Екатеринбург (GMT+5), храните UTC и конвертируйте при показе
- При добавлении новых продуктов: добавьте product_type, цены и отдельные права доступа
- Для LLM-генерации кода:
  - Следуйте архитектуре: новые фичи = новый router + keyboards + database функции + followup события
  - Не меняйте синхронность: все I/O — async
  - Сохраняйте именование и логику EventType + log_event
  - Все конфиги — через config.py/.env, не вшивайте литералы в код
