# 🔧 ИСПРАВЛЕНИЕ НА СЕРВЕРЕ

## Проблема
На сервере используется тестовый токен бота вместо продакшн токена, поэтому происходит конфликт при локальной разработке.

## Решение

### 1. Подключиться к серверу
```bash
ssh user@your-server
cd /path/to/elsesser-bot  # путь к проекту на сервере
```

### 2. Остановить бота
```bash
# Найти процесс
ps aux | grep bot.py

# Остановить (замените PID на реальный)
kill -9 PID

# Или если используется systemd
sudo systemctl stop elsesser-bot
```

### 3. Изменить .env файл на сервере
```bash
nano .env
```

Изменить строку:
```env
# БЫЛО (неправильно):
BOT_TOKEN=8526919204:AAGbiMVFRZxuho9fgORrngf_N_K8XfPz6J4

# ДОЛЖНО БЫТЬ (правильно):
BOT_TOKEN=7578063772:AAEx3yziMn-WC2Hqje6eb-FKlI_EyGD8KzQ
```

### 4. Перезапустить бота на сервере
```bash
# Если systemd
sudo systemctl start elsesser-bot
sudo systemctl status elsesser-bot

# Или напрямую
python3 bot.py
# или в screen/tmux
screen -S bot
python3 bot.py
# Ctrl+A+D для выхода из screen
```

### 5. Проверить что работает правильно
```bash
# Проверить логи
tail -f bot.log

# Должно быть в логах:
# "Bot started: @elsesser_bot" (продакшн бот, а не @elsessertestbot)
```

## После исправления

### Локальная разработка (ваш компьютер):
- Токен: `8526919204:AAGbiMVFRZxuho9fgORrngf_N_K8XfPz6J4`
- Бот: @elsessertestbot
- Команда: `py bot.py`

### Продакшн (сервер):
- Токен: `7578063772:AAEx3yziMn-WC2Hqje6eb-FKlI_EyGD8KzQ`
- Бот: @elsesser_bot (или как называется прод бот)
- Запускается автоматически через systemd

## Теперь НЕ будет конфликта!
✅ Тестовый бот работает только локально
✅ Продакшн бот работает только на сервере
✅ Никаких `Conflict: terminated by other getUpdates request`
