# AGENTS.md

## Project Overview
Telegram бот для продажи рационов питания с калькулятором калорий.

**Tech Stack:**
- Python 3.10+
- aiogram 3.22 (Telegram Bot API)
- aiosqlite (async SQLite)
- APScheduler 4.x (фоновые задачи)

**Архитектура:**
- `bot.py` — точка входа, инициализация бота и scheduler
- `database.py` — все операции с БД, модели данных, аналитика
- `handlers/` — роутеры aiogram (user, admin, calculator)
- `keyboards/` — клавиатуры и callback-данные
- `data/` — рецепты и контент (RECIPES + FMD_RECIPES)
- `followup.py` — система follow-up сообщений

**Продукты:**
- 🍽 **Калькулятор тела (рационы питания)** (14 дней) — 2000 ₽
 - Калорийность: 1200-2100 ккал
 - Завтрак + обед + ужин ежедневно
- 🥗 **FMD Протокол** (5 дней) — 1190 ₽
 - Диета имитирующая голодание
 - Отдельный продукт с независимым доступом
- 🔥 **Сушка** (14 дней) — 3000 ₽
 - Готовый рацион на ~1500 ккал
 - Завтрак + перекусы + обед + ужин ежедневно
 - Отдельный продукт с независимым доступом
- 🎁 **Комплект: Рационы + FMD** — 2990 ₽
 - Включает оба продукта со скидкой
 - При оплате активируется доступ к обоим продуктам

## Dev Environment Tips

```bash
# Создать venv
python -m venv venv
venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt

# Переменные окружения (.env)
BOT_TOKEN=your_bot_token
ADMIN_CHANNEL_ID=-100123456789
PAYMENT_AMOUNT=3000  # Рационы питания (14 дней)
FMD_PAYMENT_AMOUNT=1190  # FMD Протокол (5 дней)
BUNDLE_PAYMENT_AMOUNT=2990  # Комплект: Рационы + FMD
DRY_PAYMENT_AMOUNT=3000  # Сушка (14 дней, ~1500 ккал)
PAYMENT_DETAILS="Номер карты: 1234..."
```

## Build & Run Commands

```bash
# Запуск бота
python bot.py

# База данных создаётся автоматически при первом запуске (bot_database.db)
```

## Code Style Guidelines

- **Именование:** snake_case для функций/переменных, PascalCase для классов
- **Async:** Все DB операции и API вызовы — async/await
- **Логирование:** Использовать `logger` из logging модуля
- **HTML ParseMode:** Для форматирования сообщений Telegram
- **FSM:** aiogram FSM для многоэтапных диалогов
- **Продукты:** Отдельные функции для каждого продукта (main/fmd)
- **БД:** `product_type` в платежах для различения продуктов

## User Commands

**Основные команды:**
- `/start` — начало работы с ботом, приветствие
- `/menu` — быстрый доступ к выбору рационов
- `/fmd` — FMD Протокол (5 дней)
- `/dry` — Сушка (14 дней, ~1500 ккал)
- `/bundle` — Комплект Рационы + FMD (выгодное предложение)
- `/help` — Отдел Заботы (задать вопрос поддержке)
- `/status` — проверка статуса оплаты

**Админские команды:**
- `/admin` — вход в админ-панель (только для ADMIN_USERNAMES)

**Reply-кнопки админ-панели:**
- 📝 Редактировать рационы
- 📊 Статистика
- 📣 Управление рассылками
- 👥 Управление пользователями
- 📬 Отправить недельный отчёт
- 🔙 Выйти из админки

## Key Files Reference

| Файл | Назначение |
|------|-----------|
| `database.py` | EventType класс, log_event(), get_stats() + FMD функции |
| `handlers/admin.py` | Админка, статистика, верификация оплат (основной + FMD), ответы Отдела Заботы |
| `handlers/user.py` | /start, /menu, /help (Отдел Заботы), /status, оплата, выбор рационов + FMD протокол |
| `handlers/calculator.py` | Калькулятор калорий |
| `followup.py` | Follow-up сообщения, шаблоны |
| `config.py` | ADMIN_USERNAMES, токены, цены (PAYMENT_AMOUNT + FMD_PAYMENT_AMOUNT + BUNDLE_PAYMENT_AMOUNT + DRY_PAYMENT_AMOUNT) |
| `data/recipes.py` | RECIPES (14 дней) + FMD_RECIPES (5 дней) + DRY_RECIPES (14 дней) |

## Analytics Events

```python
from database import EventType

EventType.START_COMMAND      # /start
EventType.PAYMENT_BUTTON_CLICKED  # Нажал "Я оплатил(а)"
EventType.SCREENSHOT_SENT    # Прислал скрин
EventType.PAYMENT_APPROVED   # Оплата одобрена
EventType.PAYMENT_REJECTED   # Оплата отклонена
```

## FMD Protocol (Fast Mimicking Diet)

- **Продукт:** Диета имитирующая голодание (5 дней)
- **Цена:** 1190 ₽ (отдельный продукт)
- **Функции:**
  - Полная 5-дневная программа питания
  - Список продуктов на неделю
  - Информация о протоколе FMD
  - Независимый доступ от основных рационов
- **База данных:** `has_paid_fmd` поле, `product_type='fmd'` в платежах

## Dry (Сушка)

- **Продукт:** Готовый рацион для снижения процента жира (14 дней)
- **Цена:** 3000 ₽ (отдельный продукт)
- **Функции:**
  - Полная 14-дневная программа питания на ~1500 ккал
  - Завтрак + перекусы + обед + ужин каждый день
  - Высокое содержание белка для сохранения мышц
  - Список продуктов на неделю
  - Рекомендации по замене продуктов
  - Независимый доступ от других продуктов
- **База данных:** `has_paid_dry` поле, `product_type='dry'` в платежах

## Bundle (Комплект)

- **Продукт:** Комплект Рационы питания + FMD Протокол
- **Цена:** 2990 ₽ (экономия 1200 ₽ относительно покупки отдельно)
- **Функции:**
  - Полный доступ к рационам питания на 14 дней
  - Полный доступ к FMD Протоколу на 5 дней
  - Выгодное предложение для получения обоих продуктов
- **База данных:** `has_paid_bundle` поле, `product_type='bundle'` в платежах
- **Примечание:** При оплате комплекта автоматически активируются `has_paid` и `has_paid_fmd`

## Follow-up System

- Scheduler запускает задачи каждые 5 мин / 1 час
- `only_start` — пользователи, нажавшие только /start (24ч+)
- `clicked_payment` — нажали оплату, но без скрина (2ч+)
- Follow-up отменяются после оплаты (`cancel_user_followups`)

## Payment Flow

- **Основной рацион:** PAYMENT_AMOUNT (2000₽) → `has_paid=1`
- **FMD протокол:** FMD_PAYMENT_AMOUNT (1190₽) → `has_paid_fmd=1`
- **Сушка:** DRY_PAYMENT_AMOUNT (3000₽) → `has_paid_dry=1`
- **Комплект:** BUNDLE_PAYMENT_AMOUNT (2990₽) → `has_paid_bundle=1`, `has_paid=1`, `has_paid_fmd=1`
- Независимые продукты с отдельными платежами
- Комплект активирует доступ к рационам и FMD (не включает Сушку)
- Одинаковый процесс верификации через админ-канал

## Broadcast System (Рассылки)

Админы могут создавать и управлять рассылками через админ-панель:

**Возможности:**
- 📣 Создание рассылок с превью текста
- 📸 Добавление медиа (фото, видео)
- 🔘 Inline кнопки (ссылки и команды)
- 🎯 Выбор аудитории:
  - Все пользователи
  - Только /start (ничего не делали)
  - Отклонённые оплаты
  - Нажали оплату без скрина
- ⏰ Планирование по дате/времени (Екатеринбург, GMT+5)
- 📋 Список запланированных рассылок с возможностью отмены
- ✅ Валидация перед публикацией:
  - Текст с медиа (фото/видео): макс 1024 символа
  - Текст без медиа: макс 4096 символов
  - Проверка JSON-формата кнопок

**База данных:** таблица `broadcasts` с полями:
- `content`, `audience`, `scheduled_at`, `status`
- `created_by`, `sent_count`, `failed_count`
- `media_type`, `media_file_id`, `buttons` (JSON)

**Scheduler:** задача `process_broadcasts` каждую минуту проверяет pending рассылки

## Template System (Шаблоны)

Админы могут сохранять тексты рассылок как шаблоны для повторного использования:

**Возможности:**
- 📁 Создание шаблонов с названием
- 📸 Поддержка медиа и кнопок
- 📋 Список сохранённых шаблонов
- 📨 Использование шаблона для обычной рассылки
- 🤖 Использование шаблона для авто-рассылки
- 🗑 Удаление шаблонов

**База данных:** таблица `broadcast_templates` с полями:
- `name`, `content`, `created_by`, `created_by_username`
- `media_type`, `media_file_id`, `buttons` (JSON)

## Auto-Broadcast System (Автоматические рассылки)

Триггерные рассылки, отправляемые автоматически по событиям:

**Триггеры:**
- 👆 `only_start`: только нажали /start (ничего не делали)
- 💳 `no_payment`: нажали оплатить, но не оплатили
- ❌ `rejected`: отклонённая оплата
- 🤔 `no_screenshot`: нажали оплатить без скрина

**Настройки:**
- ⏰ Задержка отправки: 1ч, 2ч, 6ч, 12ч, 24ч, 48ч, 72ч
- 🔄 Включение/выключение авто-рассылки
- 📊 Статистика отправок
- ✅ Валидация перед публикацией (идентична обычным рассылкам)

**База данных:**
- `auto_broadcasts`: настройки авто-рассылок (+ поддержка `media_type`, `media_file_id`, `buttons`)
- `auto_broadcast_sent`: отслеживание отправленных (чтобы не слать повторно)

**Scheduler:** задача `process_auto_broadcasts` каждые 5 минут проверяет триггеры

**Формат кнопок:**
- Текст и URL/команда разделяются символом `|`
- Пример: `Хочу меню | /menu` или `Наш сайт | https://example.com`
- Кнопки-команды обрабатываются в `handlers/user.py` (callback с `/start`, `/menu`, `/help`, `/status`)

## Broadcast Chain System (Цепочки рассылок)

Цепочки рассылок — это последовательности сообщений с интерактивными кнопками, позволяющие создавать воронки продаж с ветвлением.

**Возможности:**
- 🔗 Создание многошаговых цепочек сообщений
- 🔘 Интерактивные кнопки с разными действиями:
  - ➡️ Переход к следующему шагу
  - 🔀 Переход к конкретному шагу
  - 🔗 Внешние ссылки (URL)
  - ⌨️ Команды бота
  - ⏹ Остановка цепочки (отписка)
  - 💳 Переход к оплате основного рациона
  - 🥗 Переход к оплате FMD
  - 🎁 Переход к оплате комплекта со скидкой
- ⏰ Задержка между шагами (в часах)
- 📸 Поддержка медиа (фото/видео) в каждом шаге
- 🎯 Выбор аудитории для ручного запуска

**Триггеры запуска:**
- `manual`: Ручной запуск для выбранной аудитории
- `subscription_end`: Конец подписки (через 30 дней после оплаты)
- `payment_approved`: После одобрения оплаты

**База данных:**
- `broadcast_chains`: основная информация о цепочках
- `chain_steps`: шаги цепочки (контент, медиа, задержка)
- `chain_step_buttons`: кнопки шагов (текст, действие, целевой шаг)
- `chain_user_state`: состояние пользователя в цепочке (текущий шаг, статус)
- `chain_message_history`: история отправок

**Scheduler:** задача `process_chain_messages` каждые 2 минуты проверяет отложенные сообщения цепочек

**Пример воронки:**
```
Шаг 1: "Привет! Хочешь продлить подписку?"
├── [Оплатить] → Переход к оплате
└── [Отказаться] → Шаг 2

Шаг 2: "Попробуй FMD-detox со скидкой!"
├── [Оплатить FMD] → Переход к оплате FMD
└── [Отказаться] → Шаг 3

Шаг 3: "Последний шанс: комплект за 2990 ₽!"
├── [Оплатить комплект] → Переход к оплате комплекта
└── [Нет, спасибо] → Остановка цепочки
```

## User Management (Управление пользователями)

Админы могут просматривать и управлять пользователями через админ-панель:

**Возможности:**
- 👥 Просмотр всех пользователей (с пагинацией)
- 💰 Фильтр по оплатившим рационы
- 🥗 Фильтр по оплатившим FMD
- 🎁 Фильтр по оплатившим комплект
- 🔍 Поиск по username, user_id или имени
- ❌ Сброс оплат (отдельно по продуктам или все сразу)

**Карточка пользователя показывает:**
- ID пользователя
- Имя и username
- Дату регистрации
- Статус оплат всех продуктов

**Callbacks:**
- `UserManageMenuCallback` — меню управления
- `UserListCallback` — список с пагинацией и фильтрами
- `UserActionCallback` — действия над пользователем (просмотр, сброс оплат)

**База данных:**
- `get_all_users()` — все пользователи
- `get_users_by_payment_filter()` — по типу оплаты
- `search_user_by_username_or_id()` — поиск
- `reset_user_payment()` — сброс оплаты

## Support System (Отдел Заботы)

Пользователи могут задавать вопросы через "Отдел Заботы", вопросы отправляются в канал модераторов.

**Функционал для пользователей:**
- Нажатие кнопки 💚 "Отдел Заботы" или команда `/help`
- Ввод текстового вопроса
- Получение уведомления об отправке
- Получение ответа от модератора

**Функционал для модераторов:**
- Вопросы приходят в `ADMIN_CHANNEL_ID` с кнопкой "Ответить"
- Нажатие кнопки запускает FSM для ввода ответа
- Ответ отправляется пользователю от имени бота

**States:**
- `SupportState.waiting_for_question` — ожидание вопроса от пользователя
- `SupportReplyState.waiting_for_reply` — ожидание ответа от модератора

**Callbacks:**
- `SupportReplyCallback` — кнопка "Ответить" для модераторов

**Кнопка главного меню:**
- `💚 Отдел Заботы` (заменила `❓ Помощь`)
